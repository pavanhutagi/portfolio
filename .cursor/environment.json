{
  "name": "portfolio-v2",
  "install": "nvm install && nvm use && npm ci && npx playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ]
}
